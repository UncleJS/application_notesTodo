# Dependencies / build output
node_modules/
dist/
build/
coverage/
*.log

# Env & credentials — .env.example (placeholders only) IS committed
.env
.env.*
!.env.example

# Any config carrying credentials or key material
# (data-file patterns only — must never match source like src/lib/secrets.ts)
*.pem
*.key
*.crt
*.p12
*.pfx
*credentials.json
credentials.yaml
credentials.yml
secrets.json
secrets.yaml
secrets.yml
*.secret

# Local tooling state
.claude/settings.local.json
